<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.ddbb.domain.mapper.BoardMapper">

    <!-- tb_post 테이블 전체 컬럼 -->
    <sql id="postColumns">
        id
        , title
        , content
        , writer
        , view_cnt
        , notice_yn
        , delete_yn
        , created_date
        , modified_date
    </sql>

    <!-- member 테이블 전체 컬럼 -->
    <sql id="MemberPostColumns">
        member_id
        , name
        , id
        , password
        , phone
        , email
        , nickname
    </sql>

    <!-- 게시글 저장 -->
    <insert id="member_save" parameterType="com.project.ddbb.domain.vo.MemberVO" useGeneratedKeys="true" keyProperty="member_id">
        INSERT INTO member (
        <include refid="MemberPostColumns" />
        ) VALUES (
        #{member_id}
        , #{name}
        , #{id}
        , #{password}
        , #{phone}
        , #{email}
        , #{nickname}
        )
    </insert>



    <!-- 게시글 저장 -->
    <insert id="save" parameterType="com.project.ddbb.domain.vo.BoardVO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO board (
        <include refid="postColumns" />
        ) VALUES (
        #{id}
        , #{title}
        , #{content}
        , #{writer}
        , 0
        , #{noticeYn}
        , 0
        , NOW()
        , NULL
        )
    </insert>


    <!-- 게시글 상세정보 조회 -->
    <select id="findById" parameterType="long" resultType="com.project.ddbb.domain.vo.BoardVO">
        SELECT
        <include refid="postColumns" />
        FROM
        board
        WHERE
        id = #{id}
    </select>


    <!-- 게시글 수정 -->
    <update id="update" parameterType="com.project.ddbb.domain.vo.BoardVO">
        UPDATE board
        SET
            modified_date = NOW()
          , title = #{title}
          , content = #{content}
          , writer = #{writer}
          , notice_yn = #{noticeYn}
        WHERE
            id = #{id}
    </update>


    <!-- 게시글 삭제 -->
    <delete id="deleteById" parameterType="long">
        UPDATE board
        SET
            delete_yn = 1
        WHERE
            id = #{id}
    </delete>


    <!-- 게시글 리스트 조회 -->
    <select id="findAll" resultType="com.project.ddbb.domain.vo.BoardVO">
        SELECT
        <include refid="postColumns" />
        FROM
        board
        WHERE
        delete_yn = 0
        ORDER BY
        id DESC
    </select>

    <!-- 게시글 삭제 취소 -->
    <update id="cancelById" parameterType="long">
        UPDATE board
        SET
            delete_yn = 0
        WHERE
            id = #{id}
    </update>
</mapper>